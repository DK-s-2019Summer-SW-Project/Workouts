#include<iostream>
using namespace std;

int main()
{
	int total, tshirt, sweater; // 필요한 변수를 3개 선언

	cout << ("티셔츠의 값을 입력 하시오 : ", tshirt);// 옷 값 입력
	cin >> (tshirt); // 여기 받는 부분

	if (total <= 10000)
		(tshirt + sweater)*(5 %);

	else
		(tshirt + sweater)*(15 % );

	return 0;
}